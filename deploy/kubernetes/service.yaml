apiVersion: v1
kind: Service
metadata:
  name: axiomod
  labels:
    app: axiomod
spec:
  type: ClusterIP
  selector:
    app: axiomod
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: grpc
      port: 9090
      targetPort: grpc
